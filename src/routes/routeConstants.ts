export enum routes {
  HOME = '/home',
  LOGIN = '/login',
  SUPPORT = '/support',
  PLAN_DETAIL = '/plan-detail/:planId',
  EXPLORE = '/explore',
}

export enum routeAccess {
  PUBLIC = 'public',
  PRIVATE = 'private',
}
