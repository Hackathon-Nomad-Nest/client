export enum routes {
  HOME = '/home',
  LOGIN = '/login',
  SUPPORT = '/support',
  PLAN_DETAIL = '/plan-detail/:planId',
  PRINT_PLAN_DETAIL = '/plan-detail/:planId/print',
  EXPLORE = '/explore',
}

export enum routeAccess {
  PUBLIC = 'public',
  PRIVATE = 'private',
}
